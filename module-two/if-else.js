// For an if statement to run, the condition must be true
// the if statement is used to execute a block of code if a condition is true
// the else statement is used to execute a block of code if a condition is false
// below is the expression of an if statement
// if (condition_is_true){
//     // code to be executed
// }

// Example of an if statement

// if(20 > 10){
//   console.log('20 is greater than 10');
// }

// Conditional statements
// if, else if, else, switch, ternary operator
// A conditional statement is a statement that executes a block of code if a condition is true
// truthy and falsy values
// truthy values are values that are considered true in a boolean context
// falsy values are values that are considered false in a boolean context

2 > 10 // false - falsy value
2 > 1 // true - truthy value

2 == '2' // true - truthy value'
2 === '2' // false - falsy value

// Conditional statements uses this symbols to compare values and return a boolean value
// >, <, >=, <=, ==, ===, !=, !== (Comparison operators)
// > greater than
// < less than
// >= greater than or equal to
// <= less than or equal to
// == equal to (value)
// === equal to (value and type)
// != not equal to (value)
// !== not equal to (value and type)
// && and

// 20 > 10 && 1 < 5 && 5 > 3// true

// Logical Expressions
// logical operators are used to combine multiple conditions
// they return a boolean value
// &&, ||, ! (Logical operators)

// Example:
// if they they have rice, give me rice and chicken with a salad

// if(rice && chicken && salad)){
    // give me rice and chicken with a salad
// }else if(rice && chicken && !salad){
    // give me rice and chicken
// } else if(rice && !chicken && !salad){
  // give me rice
// }

// If there is is no rice, get swallow and beef otherwise get bole and roasted chicken
// if(!rice && swallow){
    // get swallow and beef
// }else {
  // get bole and roasted chicken
// }

// Usecase problems
// 1. Paul go the restaurant get rice add chicken and salad rice if there is no rice get swallow and beef otherwise get bole and roasted chicken. 
// 2. If the user is logged in, show the dashboard otherwise show the login page
// 3. If an employee use ID to login otherwise ask the reception to clock them in
// 4. If is raining carry umbrella otherwise go sunglasses

